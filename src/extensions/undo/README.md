# Undo

Adds `/undo`, which abandons the latest user turn on the current session branch and restores its text to the input editor.

The abandoned turn remains in the session file as branch history. The command only runs while Pi is idle. Image attachments cannot be restored to the text editor.
