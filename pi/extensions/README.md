# Extensions

Pi extensions live here. Use them for custom tools, commands, hooks, UI, permission checks, session behavior, or other runtime changes.

Single-file example:

```text
extensions/my-extension.ts
```

Directory example:

```text
extensions/my-extension/
└── index.ts
```
